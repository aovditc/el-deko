define([
	'app',
	'Underscore',
	'jQuery',
	'Backbone'
], function(
	app,
	_,
	$,
	Backbone
	) {

	var galleryCollection = Backbone.Collection.extend({

		url :  

	});

	return galleryCollection;

});